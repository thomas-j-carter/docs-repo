# TODO
### `docs-build-public.yml`

Runs on PRs and main changes affecting public docs.

Steps:

1. install deps
2. run `pnpm docs:generate`
3. run `pnpm docs:build:public`
4. upload build artifact from `apps/docs-public/build`